Django>=3.0,<4.0
djangorestframework>=3.0,<4.0
djangorestframework-simplejwt>=5.0,<6.0
gunicorn>=20.0,<21.0
django-filter>=2.4,<2.5
# psycopg2-binary # Uncomment if using PostgreSQL
# Add any other dependencies used, e.g.
# Pillow
# python-dotenv
# whitenoise (for static files with gunicorn if not using nginx)
